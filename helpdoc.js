const help = () => {
    return 'Usage: npx hiqf [type/template] \n  ' +
        '  -- [type] is mean client or service \n  ' +
        '  -- [template] is mean pattern template on the repository';
}

module.exports = { help }